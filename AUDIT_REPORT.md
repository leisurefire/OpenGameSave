# 项目审计与修复记录

> 历史记录：本报告描述迁移前的 Electron 实现及当时的测试，不能作为当前 Tauri / Rust 版本的验证结论。当前迁移与待验收状态见 [MIGRATION.md](MIGRATION.md)。

日期：2026-09-05。

本报告保留各阶段审计记录，最新结论与证据见文末“提交前质量复核”。

本轮覆盖 Electron 主进程、扫描与备份 worker、renderer/shared、IPC 边界、备份/恢复/同步事务、数据库与发布脚本、依赖及现有测试。采用源码审查、并行交叉复核、真实文件系统/SQLite/worker 测试、故障注入和受控性能对比。下列已确认问题均已修复，改动保留在工作区。

## 数据安全与正确性

| 问题 | 修复及验证 |
| --- | --- |
| 目录复制到自身、子目录或通过链接别名返回源目录，会递归复制 | 检查逻辑路径与真实路径，拒绝符号链接目录；覆盖同步/异步复制及 Windows junction |
| 保留策略清理失败会删除刚完成的新快照；系统时钟回拨也可能误删新快照 | 分离快照完成与历史清理，始终保护本次快照；故障注入覆盖删除失败与时钟回拨 |
| 历史备份元数据读取缺少普通文件与大小约束 | 读取前检查文件类型、链接和 1 MiB 上限 |
| 恢复后的临时目录清理失败覆盖原始错误或成功结果 | 将清理与事务结果分离，失败时保留恢复材料；备份源缺失直接失败 |
| 注册表导入采用合并语义，回滚仍残留新建键值 | 回滚先清除失败替换内容，再导入保存的原始键树；通过模拟注册表命令验证 |
| WebDAV 提交后清理失败触发回滚，可能损坏已提交数据 | 提交后仅清理；事务目录先改名为 `.committed`，即使日志被部分删除也可在下次启动继续清理 |
| WebDAV 恢复未检查路径祖先的链接替换 | 回滚前验证目标、暂存与旧文件路径的祖先；真实 junction 测试确认外部文件不受影响 |
| WebDAV 清单允许目录大小写别名、文件/目录重叠及非法数据目录文件 | 下载前识别并拒绝这些冲突，避免在 Windows 合并到同一位置 |
| 合法的大数据库补丁超过 SQLite 参数上限 | 删除分为每批 500 项，复用插入语句；80,000 项删除通过真实数据库回归 |
| 通配符子目录回退遮蔽原目录匹配；未解析安装路径生成无效探测 | 先匹配声明的目录，再回退；保留无法解析的占位符，避免探测 `null`/`undefined` 相对路径 |
| 账号回退遇到文件消失或负修改时间失败；Epic 无关新文件遮蔽有效账号 | 忽略消失/不支持的文件，正确处理负时间戳，仅对合法账号候选比较时间 |
| Xbox 已审核候选游戏出现在列表，却无法单项查询/自动备份 | 单项查询沿用相同的已审核候选规则；保持 PGS 仅备份限制 |
| 表格、同步状态和指南页面的旧异步响应覆盖新选择 | 捕获提供方/请求代号，翻译和 IPC 返回后再次校验，丢弃过期结果 |
| 菜单窗口积累加载回调、关闭后残留父窗口监听及接受过期动作 | 加载只保留一个回调，窗口关闭时清理状态/监听，dismiss/click/resize 绑定请求代号 |

## 数据结构、GC 与热路径

| 热路径/资源 | 修复 |
| --- | --- |
| 虚拟表格每次滚动重建所有可见行 | 复用重叠行；选择更新保持已有 DOM；修正列表缩短后旧滚动值导致空视口、尺寸变化与销毁逻辑 |
| 游戏库滚动反复创建重叠卡片并请求相同封面 | 复用卡片及封面状态；离开窗口时解除图片事件并释放对象 URL |
| 快速切换详情图积累过时请求 | 排队请求只保留当前有效选择；1,000 次快速选择回归验证队列保持有界 |
| DataTable 预先创建全部操作按钮 | 仅在对应行需要显示时创建 ActionButton |
| 分数列数被取整为零，窗口计算除零 | 验证取整后的列数下限 |
| 全数据库扫描同时保留完整原始行和解析结果 | 使用 SQLite 原生迭代器逐行处理，释放已处理的原始行 |
| worker 每任务复制完整设置并继续持有输入 | 仅传递工作所需设置；postMessage 后释放输入引用，任务完成后清空上下文 |
| worker 调度反复把 Set 转换成数组 | 直接遍历查找空闲 worker |
| 重复存档路径重复计算大小；文件系统适配器共用缓存 | 路径去重；按适配器使用 WeakMap 隔离缓存；单文件直接读取当前大小 |
| 路径模板每个结果重复编译正则；最新备份先全量排序 | 每次解析复用模板格式化器；最新备份采用线性最大值查找 |
| 自动备份启用监视器时重复扫描 | 复用启动时已查询的游戏存档路径 |
| WebDAV 每处理一个文件重写并同步完整日志 | 初始清单加追加进度记录，将日志写入量从平方级降为线性；保持旧版 v1 恢复兼容，容忍崩溃时截断的末条记录 |
| WebDAV 上传裸 pipe 遗漏源错误、提前拒绝时保留源流 | 用异步迭代传播错误，统一结束/取消清理 |
| node-7z Readable 销毁不结束子进程，解压条目不消费 | 消费条目流；超时/失败终止子进程，最多等待退出 10 秒，并处理迟到的错误事件 |
| 主进程读取图片采用同步文件操作，readFile 可在文件增长后无界读取 | 放入现有有界 I/O 队列，使用异步文件句柄及预检大小加一字节的读界限，验证文件身份与变化，所有路径关闭句柄 |
| 特殊图片文件可能阻塞 open | 打开前拒绝非普通文件；平台支持时启用 O_NOFOLLOW/O_NONBLOCK；核对 dev/ino |
| 图片缓存只有字节上限，微小图片仍可产生大量条目 | 同时限制 24 MiB 和 256 条，保持 LRU 淘汰 |
| 图片响应重复复制 chunk；失败重定向未取消响应；reader 未释放锁 | chunk 保留到最终一次合并，重定向拒绝前取消 body，finally 释放 reader 锁 |
| 已淘汰的元数据请求失败后误删新请求缓存 | 失败回调仅删除仍属于自身 Promise 的条目 |
| 扫描数量限制只计匹配项，海量无关条目绕过限制 | Steam/平台目录及 Battle.net 图片缓存分别实施 20,000 项总访问预算，使用目录迭代器避免一次读取完整列表 |
| 游戏库排序每次比较构造区域比较参数；指南批量 SQL 留在主进程 | 复用 Intl.Collator，将去重、排序和指南匹配放入扫描 worker；真实 worker 回归禁止调用线程执行同步读取或数据库查询 |
| 等价数组/对象设置每次触发完整写盘及副作用 | 比较规范化后的结构内容，统一自动备份映射原型；相同值跳过写入，保留失败副作用的重试机制 |

## 依赖与数据库

`npm audit` 初次发现 3 个有漏洞的传递依赖（高/中/低各一个），均为开发/构建依赖。只更新现有兼容版本范围内的锁定版本，安装时禁用生命周期脚本：

| 包 | 原版本 | 更新后 |
| --- | --- | --- |
| `@xmldom/xmldom` | 0.8.13 | 0.8.15 |
| `fast-uri` | 3.1.5 | 3.1.7 |
| `postcss-selector-parser` | 7.1.0 | 7.1.6 |

更新后 npm 报告 0 个已知漏洞。该结果对应查询当天的公告数据，并不等于依赖不存在未知漏洞。

只读检查内置 `database/database.db`：14,010 条游戏记录，`PRAGMA integrity_check` 为 `ok`，`save_location` 和 `platform` 非空 JSON 字段无解析失败，无重复 `wiki_page_id`。未修改内置数据库及用户存档。

## 验证与性能证据

- 修改前：现有 229 项测试全部通过，说明本次修复针对已有测试未覆盖的边界。
- 最终 `npm run check` 成功：271 项测试全部通过，0 失败、0 取消、0 跳过；ESLint、配置范围内的 checkJs 类型检查、覆盖率门槛以及 main/preload/renderer 三个生产构建目标全部通过。输出保存在 `dist/audit-check-final.log`。
- 配置覆盖率范围内：行覆盖率 82.18%，分支覆盖率 77.56%，函数覆盖率 87.39%。这不是全仓所有模块的覆盖率。`git diff --check` 通过。
- 新增/扩展回归覆盖真实 worker、真实 SQLite 大补丁、Windows 文件复制与 junction、事务清理失败、截断日志、DOM 创建数、队列与监听器上限、异步返回乱序。
- 日志性能回归要求 12 个文件安装的总日志写入量小于初始清单的 3 倍，避免退化为逐文件重写完整清单。
- 排序微基准：20,000 个带数字标题，固定排列，5 轮旧/新比较，逐轮校验结果一致。旧实现中位数约 3,650 ms，复用 Collator 后约 150 ms；结果只代表该比较函数在当前机器上的表现。排序现已移出主线程。

微基准环境为 Windows、Node v26.7.0，运行时有其他审计任务并发，不能作为 Electron 整体性能提升比例。CI 使用 Node 24。本次没有进行 Electron GUI 长时间运行、真实堆快照或 GC 暂停时间采样，内存改善的证据是分配数量、队列/缓存上限与资源释放行为测试。未连接真实 GitHub/WebDAV 账户进行端到端同步；注册表回滚使用故障模拟，未操作用户真实注册表。生产构建在本机 Node 26 下出现上游 Tailwind 的 `module.register()` 弃用提示，不影响构建完成。

本轮确认的问题均已处理；上述未执行的实机和在线场景属于验证边界，不作已验证承诺。

## UI 一致性补充审计

按补充要求检查主窗口、设置、关于、弹窗、菜单与共用控件，保留原有视觉风格。

| 问题 | 修复 |
| --- | --- |
| 密码/URL 输入框与文本框表面不同，聚焦时尺寸变化，搜索框提示色和清除行为不一致 | 共用输入框样式与稳定焦点边框；四个搜索入口统一使用 search 类型，统一高度、提示色与清除按钮；刷新按钮对齐搜索框 |
| 成功状态依赖系统强调色，加载提示亮度和布局不同 | 成功图标/就绪状态采用独立语义色；统一加载组件、图标比例与状态说明；减少动画规则覆盖所有窗口 |
| 禁用按钮保留 hover 反馈，部分表单缺少键盘焦点或无障碍名称 | 统一禁用与焦点状态，补全行选择、重命名、图片等可访问名称 |
| ToggleSwitch 重新挂载后一次点击触发多次切换 | 构造时只绑定一次监听，同步 checked、disabled 与 ARIA 状态 |
| ActionButton 与原生按钮的 disabled/focus/click 行为不同 | 将属性、焦点、程序化点击转交内部原生按钮，并阻断禁用激活 |
| 下拉菜单键盘可选中禁用项，失焦后残留，长列表超出窗口 | 跳过禁用项，Tab/失焦关闭，支持键盘文字定位，限制菜单高度并显示当前活动项 |
| 关于页小字号、卡片宽度及状态样式不统一 | 调整文本层级与按钮尺寸，统一卡片边界，长说明换行且可滚动/复制 |
| 设置长说明挤窄控件，Enter 可能隐式提交刷新；弹窗长内容挤出底部按钮 | 保持控件宽度、窄窗换行、滚动及焦点留白；自动保存容器改为 main；弹窗内容与底部按钮分别约束尺寸 |
| 长菜单标签裁切，滚动菜单键盘焦点不可见，窗口宽度遗漏滚动条 | 标签换行，聚焦项滚入视口，测量宽度包含滚动条 |
| 关于页切语言把最新版本变成 Loading；下载失败无提示容器 | 版本数据与翻译状态分离；增加本地化失败反馈、busy 状态和通知容器 |
| 英文备份错误误写为还原，计数/标点/操作术语不统一，部分空状态无说明 | 对齐中英文键与插值，统一文案，补管理备份空状态及设置检测、保存失败反馈 |
| 导入导出空路径关闭窗口，自动备份间隔缺少界面边界校验 | 保留窗口并提示，间隔限定整数 1–1440 分钟，失败后恢复按钮 |
| 普通存档文件误标为文件夹，极小容量生成错误单位，日期格式不同 | IPC 异步识别 file/folder，未知类型显示通用路径；最多并发 16 次读取，保留原数组及索引；容量最小单位为 B，备份日期统一到秒 |

新增组件、页面状态、格式化、弹窗、真实文件类型和菜单键盘行为回归；完整中英文翻译键及插值校验通过。

补充验证结果：

- `npm run check` 通过：290 项测试，0 失败/取消/跳过；ESLint、配置类型检查、覆盖率门槛及三个生产构建目标通过。日志：`dist/ui-audit-check-final.log`。配置覆盖率仍为行 82.18%、分支 77.56%、函数 87.39%。
- 样式收尾后再次编译 renderer，通过；日志：`dist/ui-audit-renderer-build.log`。
- 使用最终生产 renderer、模拟 IPC 和隔离 profile，在隐藏 Electron 窗口检查中英文共 28 个页面/状态：游戏库、指南、备份、还原、GitHub/WebDAV、设置、下拉展开、关于、许可展开、更新失败、导出、备份空状态和长菜单。窗口宽度覆盖 396/450/620/650/660/800/1100px；其中较窄尺寸用于布局压力检查，不代表生产窗口最小尺寸。
- 50 项运行时断言通过，页面控制台错误为 0：包含文档无横向溢出、加载完成后隐藏、密码与文本框尺寸/表面一致、聚焦不改变高度、键盘焦点可见、图片替代文本翻译、更新错误通知与按钮恢复、Tab 关闭下拉。隐藏窗口使用浏览器焦点模拟，未抢占用户窗口。
- 实际截图另发现并修复加载容器覆盖 `.hidden` 和关于页通知继承居中排版的问题，修复后重新构建并复验。截图与测量记录位于 `dist/ui-audit/after/`，测试脚本位于 `dist/ui-audit/capture.cjs` 和 `preload.cjs`，均为本地审计产物。

上述界面验证不调用真实存档、账户或同步服务；没有替代前述长期堆/GC 分析和在线端到端验证的边界。

## 最终交叉复核

保留并复核工作区已有修复，以 290 项全部通过的测试为本次续审基线。并行检查后端事务与 IPC、前端交互与虚拟化、主进程资源生命周期；集成检查补充版本策略、更新器故障处理、只读数据库完整性、依赖和生产构建。

| 新确认问题 | 处理与回归 |
| --- | --- |
| 文件系统恢复回滚未重验目标和暂存目录祖先；失败期间替换 junction 可引导回滚修改外部文件 | 激活、回滚及清理均重验祖先；异常时保留原数据。两个真实文件系统故障注入确认外部文件不受影响 |
| Git 拉取后，损坏的元数据或非法路径阻止其他合法远程备份降为外部来源 | 逐个校验并降级合法备份，收集错误后中止保留策略及推送，避免失败批次遗留错误信任 |
| 初次加载未结束时重复打开弹窗创建多个 Electron 窗口 | 加载中的窗口也进入复用集合；模拟构造器回归中 1,000 次打开由 1,000 个窗口降为 1 个 |
| 弹窗连续选择积累导航，过期导航失败丢弃最新选择，关闭期间重开依赖即将销毁的父窗口 | 仅保留当前加载与最后一次待加载；过期失败继续加载最新值；排除关闭中窗口。覆盖加载失败重试及关闭竞态 |
| 崩溃 worker 已移出调度集合，但本机线程终止尚未完成，shutdown 提前返回 | 追踪终止 Promise，退出统一等待，包括已移出调度集合的 worker |
| 虚拟列表虽复用行/卡片，却整体移出再插回 DOM，滚动使仍可见控件丢失键盘焦点 | 共用增量节点对齐，只移除出窗节点并插入入窗节点；真实 Electron 基线复现表格与卡片两处焦点丢失 |
| 全局文案、辅助属性、页面语言、更新按钮和操作标签被旧异步响应覆盖 | 按元素、属性及请求代号校验，busy 状态立即生效，过期初始化快照丢弃 |
| 菜单异步构建尚未结束时取消或切换，旧菜单仍弹出；迟到隐藏通知清掉新触发器 | 标题栏、表格与游戏库共用请求生命周期，前后端通过 rendererRequestId 对齐通知 |
| 同步等待操作检查/翻译时可以切换目标或重复执行 | 首次 await 前固定提供方和路径并锁定控件，保存凭据共用互斥状态，所有结束路径释放状态 |
| 同步初始化的旧设置晚于用户选择返回，将界面切回旧提供方 | 使用选择代号保护初始化值；延迟初始设置回归覆盖 GitHub/WebDAV 双向切换，真实截图另明确断言实际选中的提供方 |
| 更新器下载入口同步抛错时，应用更新操作锁未释放 | 将下载调用纳入 Promise 错误处理；同步抛错、异步拒绝、并发请求及重试回归通过 |
| 超出 Number 安全整数范围的预发布编号被错误视为相等 | 数字标识按长度和字典序精确比较；覆盖相邻大整数、310 位编号及最新版本选择 |

架构复核保留现有 main/IPC/services/worker/preload/renderer 分层，集中解决边界正确性和生命周期问题。类型检查仍只覆盖 `jsconfig.check.json` 指定模块；未将局部覆盖率包装为全仓类型安全或全仓覆盖率。用户直接放入本地目录的文件继续属于本地输入信任边界，应用下载与导入的备份按外部来源处理。

截图收尾同时修正英文选择计数的单复数问题，统一使用 `Games selected: 已选 / 总数`。

## 最终验证结果

- 最终 `npm run check` 全部通过：310 项测试，0 失败、0 取消、0 跳过；ESLint、配置内类型检查、覆盖率门槛及 main/preload/renderer 三个生产构建通过。日志：`dist/audit-final-verification.log`。
- 配置覆盖率范围内：行 82.18%、分支 77.56%、函数 87.39%。`git diff --check` 通过。
- 重新查询 `npm audit` 为 0 个已知漏洞，结果位于 `dist/audit-resume-dependencies.json`。只读检查内置数据库仍为 14,010 条记录，完整性为 `ok`，非空存档路径及平台 JSON 无解析错误；没有修改内置数据库。
- 真实 Electron 42.8.1 / Chromium 148 / Node 24.18.1 的隐藏窗口压力验证：游戏库和备份视图各 10,000 条模拟数据，720 次滚动、重复搜索及页面切换，预热后 5 轮强制 GC。9 项断言全部通过，控制台错误为 0。采样时可见/保留卡片为 35、表格行为 16；DOM 节点始终 3,034，监听器始终 336。累计创建 1,409 个图片对象 URL，各采样点未释放数量为 0。
- 压力场景强制 GC 后 JS 使用堆由 7,612,892 字节变为 7,455,652 字节（约 7.26→7.11 MiB），没有持续增长；backing storage 由 425,003 字节变为 784,216 字节。表格与游戏库滚动焦点验证均由修复前失败变为通过。完整数据：`dist/ui-audit/complete-stress/stress.json`；脚本：`dist/ui-audit/stress.cjs`、`stress-body.cjs`、`prepare-stress.cjs`。
- 最终生产 renderer 的中英文 28 个页面/状态完成截图和尺寸复验，52 项运行时断言全部通过，控制台错误及未模拟 IPC 均为 0。包含 WebDAV 实际选中状态、密码/文本框一致性、焦点尺寸稳定、下拉键盘关闭、更新失败反馈和文档横向溢出。已人工检查备份、设置及中文 WebDAV 截图。产物：`dist/ui-audit/complete-ui/measurements.json` 和同目录 PNG；脚本：`dist/ui-audit/capture-complete.cjs`。脚本等待同步页加载后就绪再执行提供方切换，避免将懒加载未完成误认为页面已验收。

这次补充了真实 renderer 的短程 GC 后内存采样，仍没有执行长期堆快照对比、自然 GC 暂停分布或整机 RSS 基准。不能将模拟数据下的有界结果视为所有实际游戏、驱动和账户环境的性能保证。GUI 使用隔离 profile 和模拟 IPC；真实 GitHub/WebDAV 账户同步、签名安装升级和真实注册表恢复仍未做端到端验收。所有已确认问题均已修复，源码及新增回归保留在工作区，未提交或发布。

## 提交前质量复核

日期：2026-09-06。

复核工作区差异及相关调用链，重点检查备份保留策略、文件系统和注册表回滚、WebDAV 日志兼容及提交后清理、远程元数据信任、worker/窗口资源释放、IPC 参数传递、虚拟列表复用及异步界面状态。本次未发现新的阻断提交问题，保留已有实现和回归测试。

重新运行 `npm run check` 成功：310 项测试全部通过，0 失败、取消或跳过；ESLint、配置内类型检查、覆盖率门槛及三个生产构建目标通过。配置覆盖率为行 82.18%、分支 77.56%、函数 87.39%。`git diff --check` 通过。日志保留于本地 `dist/review-commit-check.log`，不纳入版本控制。首次运行因沙箱禁止测试子进程启动而失败，获得自动审批后重跑通过。构建存在 Node `module.register()` 弃用提示，未影响构建结果。

按依赖更新、备份与恢复、云端同步、游戏库扫描与封面、主进程生命周期、界面交互与本地化、审计文档分组提交；源码和对应回归测试同组。各阶段“未提交”描述为当时状态。

本次没有重跑前述 GUI 压力/截图、依赖漏洞查询或真实账户端到端验证；这些记录属于先前审计证据。测试使用模拟界面与故障注入的部分不能替代真实账户同步、签名安装升级和注册表恢复验收。
