# 扫描与菜单性能

当前随仓库分发的数据库约 6.3 MiB，包含 14,010 条游戏记录。完整数据库扫描需要读取所有规则；主要应关注规则展开产生的文件系统访问、注册表探测和重复解析，而不是仅优化 SQLite 读取。

## 本轮实现

- 普通扫描先匹配安装目录与候选游戏，只解析需要处理的完整存档定义。完整扫描仍处理全部规则，包括未安装游戏。
- 每次只读扫描独享一个 `ScanContext`，复用目录枚举、元数据、目录统计和规则展开结果。缓存限制条目数；超过容量后继续流式处理，不跳过规则或文件。
- 发生实际目录读取前，重新验证完整祖先路径，避免把扫描缓存当作符号链接或 junction 的授权依据。备份、还原和删除仍使用实时校验，不使用展示缓存。
- 占位符正则预编译，动态账号排序复用排序键，减少重复读取文件元数据。
- 注册表精确键存在性检查使用原生只读查询，避免每个规则都启动 `reg.exe`。注册表导入、导出和回滚保持原来的事务流程。
- 扫描循环检查退出取消标志，正在退出时停止只读发现工作。
- 原生菜单在主窗口就绪后预创建，之后反复更新和显隐同一个 WebView。菜单使用独立请求序号，拒绝旧的尺寸回报、焦点事件和点击。关闭原生阴影以移除 Windows 无装饰窗口的额外白边。

## 如何验证

`full_catalog_scan_reuses_io_without_skipping_uninstalled_rules` 使用 12 款未安装游戏和 97 个共享文件，对比启用与禁用缓存的完整扫描：输出必须完全相同，同时检查目录读取和严格路径检查次数下降。另有扫描后备份读取新内容、退出取消、目录被替换为链接和原生注册表查询兼容性测试。

该回归场景实测结果：

| 指标 | 未使用扫描缓存 | 使用扫描缓存 |
| --- | ---: | ---: |
| 实际目录枚举次数 | 37 | 2 |
| 严格路径检查次数 | 2,631 | 120 |
| 检出的游戏数 | 12 | 12 |

这是共享路径场景的访问次数对比，不能直接换算成所有电脑上的总耗时提升。83 项 Rust 回归测试已通过。

本机 Windows 发布构建的只读采样：普通扫描发现 7 款游戏，约 2.46 秒；完整扫描覆盖 14,010 条规则，发现 38 款有存档的游戏，约 36.8 秒。这是本机基线，不是跨版本提速比例。

`npm run smoke:desktop -- --conflict-close --watcher` 使用隔离数据库、存档和 WebView2 配置验证原生窗口交互。使用 `--binary <exe>` 指定发布版。

评估真实扫描性能时，应记录规则数量、候选游戏数、目录读取数、元数据查询数、首批结果时间和总耗时；分别比较首次与重复扫描。机器同时执行其他构建时的耗时不适合直接用作性能提升比例。

## 后续优化顺序

1. 把存档发现与精确容量统计分开，先显示已发现游戏，再在后台补齐大小。
2. 基于数据库版本、安装根目录和账户信息缓存列表，用文件变化使相关游戏失效；备份和还原前始终重新验证。
3. 对重复刷新合并请求，并在有真实磁盘测量后确定有限并发数。
4. 为文件监视的溢出、丢失事件和目录替换安排局部重扫兜底，避免长期缓存遗漏存档。

Rust 在 Windows 上可复用目录枚举返回的元数据，详见 [DirEntry 文档](https://doc.rust-lang.org/std/fs/struct.DirEntry.html)。已运行的阻塞任务需要协作取消，详见 [Tokio 文档](https://docs.rs/tokio/latest/tokio/task/fn.spawn_blocking.html)。Windows 目录通知溢出后需要重新枚举，详见 [Microsoft 文档](https://learn.microsoft.com/zh-cn/windows/win32/api/winbase/nf-winbase-readdirectorychangesw)。
